public class Gpu {
    private double frequency; //Частота видеопроцесара
    private int blocks; //Количество блоков графики
    public Gpu(double frequency, int blocks) {
        this.frequency = frequency;
        this.blocks = blocks;
    }
}
